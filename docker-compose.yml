services:
  pipeline:
    build: .
    volumes:
      - ./data:/app/data:ro
      - ./:/app/output
    command: python run_solution.py --data /app/data --out /app/output/predictions.csv --dynamic-dates
