services:
  pipeline:
    build: .
    environment:
      - PYTHONPATH=/app
    volumes:
      - ./data:/app/data:ro
      - ./:/app
    command: python run_solution.py --data /app/data --out /app/predictions.csv --dynamic-dates
